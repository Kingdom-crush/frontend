# HDL Web Demo 开发记录

本文档用于记录每次对话推进的时间和主要修改内容。后续每轮继续开发时，都先补充或更新本记录。

## 2026-05-03 19:00 +08:00

- 按要求新增本开发记录文档，后续每次对话持续维护。
- 继续推进 B/S 化 demo 的产品化改造：当前主流程已覆盖后端静态托管前端、源码目录预检、创建工程、运行检查、读取结果、生成 HTML 报告、任务中心日志查看。
- 上一轮已补充后端烟测脚本 `hdl-web-backend/smoke-test.ps1`，用于回归验证完整流程。
- 上一轮已修复 Windows 下引擎输出日志中文路径乱码问题，后端默认使用 `GBK` 读取命令行输出，结果文件仍按 UTF-8 解析。
- 上一轮已将结果中的等级/分类代码映射为可读名称，例如 `L001/C014` 映射为 `严重错误/14 操作符`。
- 上一轮已在后端模式下禁用未接入的 Word/PDF/Excel/WPS 报告格式，避免用户误以为导出失败。

## 2026-05-03 19:06 +08:00

- 新增后端工程元数据保存能力：`POST /api/projects/{projectId}` 可更新工程名称、分析人、规则集和语言。
- 后端工程模型新增 `analyst` 字段，并兼容旧版 `projects.tsv` 记录。
- 前端“保存工程”在后端模式下已接入真实 API，可保存工程名称、分析人和当前规则集。
- 前端禁止直接修改已创建后端工程的源码目录；如用户改动源码路径，会提示需要新建工程，避免后端引擎工程配置和前端显示不一致。
- 烟测脚本 `hdl-web-backend/smoke-test.ps1` 增加工程更新接口断言，覆盖创建、保存、分析、结果解析、报告生成、日志持久化和静态前端访问。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 19:12 +08:00

- 增强后端规则元数据解析：通过 `toif/toolcheckers/*Reference.xml` 将结果中的 `R_...` / `new_R_...` 规则别名映射回规则集中的 `CID...` 规则 ID。
- 结果接口新增规则详情字段，包括 `ruleId`、`ruleName`、`ruleMessage`、`ruleDescription`、`ruleSpecification`。
- 前端右侧帮助区优先展示真实规则名称、规则 message、description 和 specification 示例，不再只显示结果文件里的简短检测消息。
- 烟测脚本新增规则详情断言，确认规则别名可解析、规则名称不为空。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 20:02 +08:00

- 为后端结果接口增加分页、搜索和排序能力：`GET /api/projects/{projectId}/results?page=1&pageSize=100&search=...&sort=file&dir=asc`。
- 结果接口保持向后兼容：不传分页参数时仍返回全部结果；传分页参数时额外返回 `pagination` 对象。
- 前端后端模式新增检查结果分页条，可切换上一页/下一页、跳转页码和设置每页条数。
- 前端后端模式下搜索按钮、清除按钮、回车搜索和表头排序改为调用后端分页查询，避免大工程一次性在浏览器侧过滤全量数据。
- 烟测脚本新增分页和后端搜索断言，验证分页限制、总数和规则号搜索。
- 后端 README 更新结果分页查询说明。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 20:33 +08:00

- 实现后端自定义规则集保存接口：`POST /api/rulesets`，从基础规则集复制 XML，并根据前端勾选的规则设置 `enable=true/false`。
- 自定义规则集会保存到 `hdl-checker20260422/rulesets/user/custom_<name>.xml`，并登记到 `rulesets.xml`，因此可直接作为 `analyze -r <rulesetId>` 的真实规则集使用。
- 实现自定义规则集删除接口：`DELETE /api/rulesets/{customRulesetId}`，仅允许删除 `custom_` 前缀的规则集，避免误删内置规则集。
- 前端后端模式下“新建规则集/保存自定义规则集”已接入真实 API；保存后自动切换到后端规则集并可用于运行检查。
- 烟测脚本新增自定义规则集创建、启用规则数量、使用自定义规则集分析、删除清理的验证；确认测试规则集不会残留在引擎规则目录或 `rulesets.xml`。
- 后端 README 更新自定义规则集 API 说明。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 21:21 +08:00

- 修复分页模式下“全部违反/全部不违反/全部未确认”只作用于当前页的问题。
- 后端审查接口 `POST /api/projects/{projectId}/violations` 新增 `allMatching=true`，可按当前后端搜索条件和状态过滤条件批量更新所有匹配结果。
- 前端后端模式下的“全部”审查按钮改为调用后端批量审查接口，完成后刷新当前分页结果。
- 单条结果和当前页多选结果仍保留按 ID 更新的原有逻辑。
- 烟测脚本新增按规则号搜索后批量标记审查状态的验证，确认更新数量与后端筛选数量一致。
- 后端 README 更新批量审查接口说明。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 21:33 +08:00

- 修复后端分页模式下审计进度统计只容易被前端当前页数据影响的问题。
- 后端结果接口新增 `auditSummary`，同时返回全工程审计统计和当前筛选条件下的审计统计。
- 前端后端模式下的报告卡片、底部统计和分页条改为优先使用后端 `auditSummary`，显示全工程审计进度。
- 烟测脚本新增初始审计统计、分页审计统计、筛选审计统计和批量审查后的统计断言。
- 后端 README 更新 `results` 接口的 `auditSummary` 字段说明。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 21:49 +08:00

- 修正检查结果浏览视图中的“按问题级别”逻辑：由原来的审计状态分组改为按规则严重级别分组。
- 新增“按规则类别”结果分组，用于覆盖客户缺项汇总中按类别组织检查结果的要求。
- 检查结果明细表新增“规则”和“级别”列，表头排序支持按规则号和问题级别排序。
- 后端结果接口补充 `level`/`severity`、`category`/`cat` 排序键，前端后端模式点击对应表头时可走服务端排序。
- 烟测脚本新增后端 `level` 和 `category` 排序查询断言，README 同步更新支持的排序键。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 22:04 +08:00

- 后端 `source-tree` 接口新增源码统计 `stats`，包括 HDL 文件数、约束文件数、总代码行、有效行数、注释行数和注释率。
- 后端源文件节点新增 `kind` 字段，用于区分 `hdl`、`constraint`、`other`、`folder`，便于前端源文件列表显示文件类型。
- 前端后端模式下的“有效行数”“注释率”“源文件个数”改为优先使用后端真实源码扫描结果，不再依赖 demo 假数据。
- 烟测脚本新增源码树分类、HDL 文件数、有效行数、注释率字段断言；后端 README 更新 `source-tree` 返回字段说明。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 22:14 +08:00

- 新增后端 `GET /api/projects/{projectId}/design-summary`，对 Verilog/VHDL 源码做轻量扫描，提取模块/实体、实例关系、层次化调用和疑似 IP/黑盒项。
- 前端后端模式下“层次化调用与 IP 识别”面板改为优先显示后端扫描结果，没有识别到 IP 时显示明确的“无黑盒/IP”状态。
- 后端健康能力列表新增 `design-summary`，README 增加接口说明并标注该能力是 best-effort 扫描，不替代完整 elaboration。
- 烟测脚本新增模块/实体识别和 `top` 层次显示断言。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-03 22:35 +08:00

- 后端自定义规则集保存接口新增 `ruleLevels` 参数，可把规则等级写回规则 XML 的 `conLevel`，并同步更新 `priority`。
- `ruleLevels` 支持 `L001`/`L002`，也支持前端下拉框传入包含“严重”或“一般”的中文等级标签。
- 后端规则列表返回值新增 `levelId` 和 `categoryId`，便于前端和自动化测试精确确认 XML 中的原始等级/分类。
- 前端后端模式保存自定义规则集时，已把每条规则的等级下拉值一起提交到后端。
- 烟测脚本新增规则等级覆盖断言，确认 `CID9583` 可被保存为 `L002`。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-04 00:14 +08:00

- 新增后端 `SourceSelectionStore`，按工程持久化源文件纳入/排除状态到 `source-selection.tsv`。
- 新增 `GET/POST /api/projects/{projectId}/source-selection`，支持按单个路径或批量 include/exclude 更新源文件选择状态。
- `source-tree` 返回的文件节点新增 `included` 和 `explicitSelection` 字段，前端可区分默认纳入和用户显式调整。
- 前端后端模式下“源文件列表管理”新增操作列，可对 HDL/约束文件执行“纳入/排除”切换，并刷新项目树、概要统计和源文件列表。
- README 明确说明当前源文件选择状态先驱动前端管理视图，实际影响引擎分析输入是后续独立工作。
- 烟测脚本新增源文件排除和重新纳入的持久化断言。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-04 00:24 +08:00

- 将源文件纳入/排除状态接入真实分析流程：存在源文件选择状态时，分析任务会先生成筛选源码镜像，再刷新引擎项目配置并运行分析。
- 筛选源码镜像保存在 workspace 下的 `source-mirrors/{projectId}/`，避免被引擎 `create -p <projectPath>` 覆盖工程目录时删除。
- `EngineRunner` 在分析任务日志中记录 `[source-selection]` 和镜像路径，便于任务中心追踪本次分析使用的是筛选后的源码输入。
- README 更新源文件选择对分析输入的实际影响说明。
- 烟测脚本新增分析日志断言，确认分析流程确实使用了 `source-mirrors` 镜像目录。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-04 00:29 +08:00

- 修复工程删除时未清理筛选源码镜像的问题。
- `ProjectStore.deleteProject` 现在会同时删除工程目录和 workspace 下的 `source-mirrors/{projectId}`。
- 烟测脚本新增删除工程后的工程目录、源码镜像目录清理断言，避免长期运行后留下无主文件。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-04 14:57 +08:00

- 新增后端 `ReportTemplateService` 和 `GET /api/report-templates?format=html`，提供内置 `default_html` 模板，并扫描 engine/workspace 下的 `report-templates/` 目录。
- 前端后端模式连接成功后会加载服务器报告模板，分析报告页的模板下拉框改为使用后端模板列表。
- 生成 HTML 报告时，前端会把当前模板 ID 传给后端。
- 后端报告任务会在任务日志中记录 `[report-template]`，并在 `report/report-metadata.json` 中保存工程、格式和模板元数据。
- README 更新报告模板接口、模板记录方式以及当前引擎尚未支持真实模板渲染插件的边界说明。
- 烟测脚本新增报告模板能力、默认模板列表、模板响应、任务日志和 `report-metadata.json` 断言。
- 验证通过：`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-05 21:19 +08:00

- 新增后端服务器目录浏览能力：`HDL_SOURCE_ROOTS` 配置可指定允许前端浏览的源码根目录，默认包含后端当前目录和 workspace。
- 新增 `GET /api/filesystem`，支持列出允许根目录、浏览指定服务器目录、返回父目录和轻量文件类型，供 B/S 模式下新建工程选择源码目录使用。
- 后端健康检查新增 `sourceRoots` 和 `filesystem-browser` 能力标识，便于前端按能力启用或禁用目录浏览。
- 前端“新建后端工程”弹窗新增“浏览...”按钮和服务器目录浏览面板，可进入目录、上一级、刷新并选择服务器源码目录。
- README 更新 `HDL_SOURCE_ROOTS`、目录浏览 API 和当前 B/S 路径边界说明。
- 烟测脚本新增目录浏览能力、根目录列表和 `sample-src/top.v` HDL 文件枚举断言。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-07 03:21 +08:00

- 清理前端项目管理页里的旧 demo 占位入口：删除“打开分析配置”“导入源文件”和“选择路径”按钮，避免用户误以为已创建后端工程的源码目录可以在项目页直接替换。
- 删除顶部工具栏和配置菜单中的“打开配置”入口，保留“项目管理”和“源文件列表管理”等当前真实可用路径。
- 将项目属性里的源码目录改为只读显示；新建工程时仍通过后端弹窗和服务器目录浏览选择源码目录。
- 去掉未再使用的配置工具栏图标 CSS，并把若干“模拟/demo”状态提示改成更中性的产品化提示。
- 更新电路图弹窗默认说明：后端模式优先读取同名 JSON，缺失时才显示静态预览。
- 对 `build-web-apps` 插件做了可用性判断：当前会话没有暴露可直接调用的具体 skill 文件，本轮按其通用启发执行了“删除死入口、收敛用户流程、让状态更明确”的清理。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-08 00:07 +08:00

- 删除页面最上方菜单栏，保留工具栏和工程/规则/报告等主操作入口，减少重复入口。
- 优化后端自动探测逻辑：优先探测当前地址和常用端口，其余候选端口改为短超时并行探测，避免打开网页时被 18080-18099 串行等待拖慢。
- 顶部工具栏将“加载项目”改为“刷新工程”，“保存项目”改为“保存工程属性”，并增强刷新/保存后的状态反馈。
- 新增多工程操作选择弹窗：当存在多个工程时，点击“运行检查”或“查看报告”会先要求用户选择目标工程，再执行对应操作。
- 项目管理页的“源文件列表管理”改为接近原工具“分析配置”的双栏布局：左侧显示源文件列表和纳入/排除操作，右侧汇总当前排除项。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-08 00:13 +08:00

- 修复“分析配置”源文件列表中纳入/排除按钮不明显或未显示的问题，将行布局调整为“路径 / 状态 / 操作”三列。
- 右侧“排除/过滤配置”列表新增“纳入”按钮，可直接把已排除文件恢复到下次分析输入。
- 保持后端模式继续调用 `source-selection` 接口；本地演示数据也补充了前端侧纳入/排除状态切换。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。

## 2026-05-08 00:56 +08:00

- 删除最顶部窗口标题栏，去掉“结果审查 - test/out/test.xml - SunwiseHDLChecker”等静态标题信息。
- 删除工具栏右侧重复的“运行检查”和“查看报告”按钮，保留左侧主工具栏入口。
- 将检查执行进度从规则集管理页移到分析报告页，运行检查时自动切换到分析报告页显示进度和日志。
- 左侧检查结果浏览视图改为允许长规则名称自动换行，减少侧栏宽度变窄时文字被截断的问题。
- 右侧规则说明面板默认折叠，点击检查结果时自动展开；面板顶部精简为“规则说明 + 折叠”，移除无效图标按钮和选项卡。
- 验证通过：`node --check .\demo\app.js`、`node --check .\demo\backend-bridge.js`、`.\hdl-web-backend\build.ps1`、`.\hdl-web-backend\smoke-test.ps1`。
